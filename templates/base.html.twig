<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Playbox Productions{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    {# Bootstrap 5 CDN for now; switch to Encore/Webpack later #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384..." crossorigin="anonymous">
    
    {% block stylesheets %}{% endblock %}
</head>
<body>

    <header class="bg-dark text-white">
        <nav class="navbar navbar-expand-lg navbar-dark container">
            <a class="navbar-brand" href="{{ path('app_home') }}">Playbox</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav" aria-controls="mainNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="mainNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_home') }}">Home</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_about') }}">About</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_classes') }}">Classes</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_shows') }}">Shows</a></li>
                </ul>
            </div>
        </nav>
    </header>

    <main class="container py-4">
        {% block body %}{% endblock %}
    </main>

    <footer class="bg-light text-center text-muted py-4">
        <div class="container">
            <p class="mb-1">&copy; {{ "now"|date("Y") }} Playbox Productions</p>
            <div>
                <a href="mailto:info@playbox.org" class="text-reset me-3">info@playbox.org</a>
                <a href="#" class="text-reset me-3">Facebook</a>
                <a href="#" class="text-reset">Instagram</a>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384..." crossorigin="anonymous"></script>
    {% block javascripts %}{% endblock %}
</body>
</html>
